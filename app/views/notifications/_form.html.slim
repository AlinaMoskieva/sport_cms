.scaffold-form
  = simple_form_for(notification) do |f|
    .form-inputs
      = f.input :comment_id
      = f.input :user_id

    .form-actions
      = f.button :submit
      '  or
      = link_to 'Back', :back

